{
  "name": "hardhat-project",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.1",
    "@openzeppelin/contracts": "^4.8.1",
    "dotenv": "^16.0.3",
    "hardhat": "^2.12.7",
    "hardhat-deploy": "^0.11.25"
  },
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat deploy --export-all deployments/index.json"
  },
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
